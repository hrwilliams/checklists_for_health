<%= render('layouts/errors', :object => @physical_task) %>

<%= form_for [@week, @physical_task], remote: true do |f| %>
  <%= f.label :description %>
  <%= f.text_field :description %>
  <p><%= f.submit %></p>
<% end %>
