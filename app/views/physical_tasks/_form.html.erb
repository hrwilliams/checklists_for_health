<%= render('layouts/errors', :object => @physical_task) %>

<%= form_for [@week, @physical_task] do |f| %>
<div class="form-group">
  <%= f.label :description %>
  <%= f.text_field :description %>
  </div>
  <p><%= f.submit %></p>
<% end %>
